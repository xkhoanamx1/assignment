# This directory is archived.

The canonical Next.js application lives at the repository root in `app/`. This `web/` directory is the older Gemini-only prototype and is **not** the deploy target.

Please read the project documentation at [`../README.md`](../README.md).